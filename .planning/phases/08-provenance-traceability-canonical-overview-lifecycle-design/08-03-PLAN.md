---
phase: 08-provenance-traceability-canonical-overview-lifecycle-design
plan: 08-03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - scripts/check-banned-classes.mjs
  - frontend/components/calibration/CalibrationWorkspace.tsx
  - frontend/components/settings/SettingsWorkspace.tsx
  - frontend/components/contradictions/ContradictionWorkspace.tsx
  - frontend/components/functions/FunctionsWorkspace.tsx
  - frontend/components/developments/DevelopmentsWorkspace.tsx
  - frontend/components/missing-signals/MissingSignalsWorkspace.tsx
  - frontend/components/intelligence/AthenaWorkspace.tsx
  - frontend/components/observability/ActivityStreamWorkspace.tsx
  - frontend/components/signals/SignalList.tsx
autonomous: false
requirements:
  - REQ-P8-08
  - REQ-P8-09
  - REQ-P8-10
  - REQ-P8-11
  - REQ-P8-12
  - REQ-P8-13
  - REQ-P8-18
  - REQ-P8-19
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Zero banned utilities remain in any workspace component, drawer, or modal under frontend/components (excluding the canonical metaradar.tsx reference, which is token-clean by construction) — enforced by a CI-able grep gate script (REQ-P8-11, D-08-09)."
    - "Every swept workspace uses the canonical SectionTitle (eyebrow/title/detail) and the .panel / .badge / .muted classes from the Overview/Lifecycles design system; no per-page heading sizes or arbitrary fonts remain (REQ-P8-08, REQ-P8-10)."
    - "The global font stack is the canonical Arial/Helvetica/sans-serif; monospace appears only on IDs, fingerprints, and technical identifiers — never on prose, headings, or UI labels (REQ-P8-09)."
    - "Light and dark are both real semantic token themes with no dark-invert artifacts; theme state lives once in the root ThemeProvider and persists across client navigation, refresh, drawer open/close, workspace changes, and direct URL navigation (REQ-P8-11, REQ-P8-12)."
    - "Drawers and modals (EvidenceDrawer, Confluence inspector) use the same typography, surface hierarchy, borders, spacing, buttons, badges, and theme tokens as Overview/Lifecycles and remain readable in both themes (REQ-P8-13)."
    - "Settings shows the CONFIGURATION_ERROR credential status (consuming the configuration_error_message surfaced by Plan 08-02) with the official location to obtain the key (REQ-P8-07 cross-check)."
    - { statement: "The sweep preserves compliant token references (CSS var() usages and .panel/.badge/.muted classes) exactly; it does not introduce a third token set or modify the canonical reference pages.", verification: backstop }
    - { statement: "No component introduces its own theme state; the root ThemeProvider remains the single source of truth (no per-page theme duplication).", verification: backstop }
  artifacts:
    - scripts/check-banned-classes.mjs
    - frontend/components/calibration/CalibrationWorkspace.tsx (token-clean)
    - frontend/components/settings/SettingsWorkspace.tsx (token-clean + credential status)
    - frontend/components/contradictions/ContradictionWorkspace.tsx (token-clean)
    - frontend/components/functions/FunctionsWorkspace.tsx (token-clean)
    - frontend/components/developments/DevelopmentsWorkspace.tsx (token-clean)
    - frontend/components/missing-signals/MissingSignalsWorkspace.tsx (token-clean)
    - frontend/components/intelligence/AthenaWorkspace.tsx (token-clean)
    - frontend/components/observability/ActivityStreamWorkspace.tsx (token-clean)
    - frontend/components/signals/SignalList.tsx (token-clean + SectionTitle)
  key_links:
    "globals.css semantic tokens (light :root + .dark) <-> every swept workspace component"
    "metaradar.tsx canonical exports (SectionTitle, Card/.panel, Badge/.badge, .muted, .eyebrow) <-> swept workspace headers and cards"
    "scripts/check-banned-classes.mjs <-> every .tsx under frontend/components (regression gate)"
    "frontend/lib/api.ts configuration_error_message payload (Plan 08-02) <-> SettingsWorkspace credential display"
  prohibitions:
    - { statement: "No banned utilities (slate/hex utilities or dark-paired hardcoded variants) may be introduced or reintroduced in any workspace component.", flagged: unverified }
    - { statement: "No modifications to frontend/components/metaradar.tsx (canonical reference) or to the globals.css token definitions (UI-SPEC section 0 non-goals).", flagged: unverified }
    - { statement: "No new design system, third token set, or parallel UI kit; existing shadcn primitives and hand-rolled .panel/.badge classes stay as-is.", flagged: unverified }
    - { statement: "No per-page theme state or duplicate theme providers; the root ThemeProvider + localStorage key remains the single source of truth.", flagged: unverified }
---

<objective>
Canonicalize the entire frontend onto the Overview/Lifecycles design system: replace every banned utility class and hardcoded color in the nine remaining workspace components with semantic tokens, apply the canonical SectionTitle/panel/badge typography hierarchy, enforce the single global font stack with monospace reserved for identifiers, and prove theme persistence and readability in both themes via the full manual verification matrix.

Purpose: Plans 08-01/08-02 made the backend honest; the frontend is still riddled with hardcoded slate/hex utilities (100+ occurrences) that break light mode and violate the UI-SPEC banned list — the observed "returns to dark" bug persists until components stop pinning light/dark pairs. D-08-08/09/10/11/12/13 require every workspace, drawer, and modal to consume the same token system, with theme state living once in the root provider.

Output: A CI-able banned-class grep gate, nine token-clean workspace components (CalibrationWorkspace, SettingsWorkspace with credential status, ContradictionWorkspace, FunctionsWorkspace, DevelopmentsWorkspace, MissingSignalsWorkspace, AthenaWorkspace, ActivityStreamWorkspace, SignalList), typography/font enforcement, and a human-approved UI-SPEC section 10 manual matrix in both themes.
</objective>

<execution_context>
@C:/Users/OM Prakash/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/OM Prakash/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-provenance-traceability-canonical-overview-lifecycle-design/08-CONTEXT.md
@.planning/phases/08-provenance-traceability-canonical-overview-lifecycle-design/08-RESEARCH.md
@.planning/phases/08-provenance-traceability-canonical-overview-lifecycle-design/08-PATTERNS.md
@.planning/phases/08-provenance-traceability-canonical-overview-lifecycle-design/08-UI-SPEC.md
@.planning/phases/08-provenance-traceability-canonical-overview-lifecycle-design/08-VALIDATION.md
@.planning/phases/08-provenance-traceability-canonical-overview-lifecycle-design/08-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Banned-class grep gate script + full token sweep of CalibrationWorkspace (TRACER)</name>
  <precondition>Plan 08-02 complete: frontend/types/api.ts regenerated with configuration_error_message/last_attempted and the frontend builds clean.</precondition>
  <reversibility rating="reversible">Component edits are local UI changes; reverting touches the single workspace file plus the gate script.</reversibility>
  <files>scripts/check-banned-classes.mjs, frontend/components/calibration/CalibrationWorkspace.tsx</files>
  <read_first>
    - frontend/app/globals.css lines 31-88 (semantic token vars, both themes) and lines 7-27 (@theme inline mapping)
    - frontend/components/metaradar.tsx lines 110-148 (Badge/Card/SectionTitle primitives), 1089-1149 (LifecyclePage — the canonical token-var() page pattern), 760-938 (DashboardPage)
    - frontend/components/calibration/CalibrationWorkspace.tsx (full file — heaviest offender with 100+ banned matches; verify the canonical .calibration-widget-card/.role-pill/.star-btn/.before-after-panel/.comparison-card classes exist and are applied)
    - 08-UI-SPEC.md section 1 (Design Token Reference, lines 18-101), section 1.4 (Canonical CSS Classes), section 4 (sweep table, lines 171-182)
    - 08-RESEARCH.md "Frontend Design System & Theme Changes" sweep procedure (line 323) and "Pitfall 5" (lines 423-427)
    - 08-PATTERNS.md "Workspace components" section (lines 506-552) — the shared SectionTitle/Card/loading/error/empty pattern
  </read_first>
  <behavior>
    - Test 1: the gate script scans a file containing a banned utility and exits non-zero listing the file and line; it exits 0 on a token-clean file; it ignores comment lines.
    - Test 2: CalibrationWorkspace.tsx contains zero banned utilities and zero fallback/fabricated values.
  </behavior>
  <action>
    Create scripts/check-banned-classes.mjs FIRST (RED): a zero-dependency Node script (node >=20, uses fs + path only) that takes file paths as arguments (or a directory to walk), scans each .tsx file line by line, skips comment-only lines, and reports every line containing any member of the UI-SPEC section 1 banned list (the slate utility prefixes, hex-bracket utilities, and dark-paired hardcoded variants) with file:line output; exits 1 when violations are found, prints a summary when clean. Verify the script flags a known-bad fixture then passes on a token-clean file. Run it against CalibrationWorkspace.tsx and confirm it currently reports violations (RED).

    Then sweep (GREEN):

    1. CalibrationWorkspace.tsx: replace every banned utility with semantic tokens per the UI-SPEC section 4 sweep table — surfaces to the token variables, borders to the border token, text to foreground/muted-foreground tokens, chips to the neutral badge class; ensure the canonical calibration classes (.calibration-widget-card, .role-pill, .role-pill-active, .star-btn, .star-active, .submit-feedback-btn, .recalibrate-btn, .before-after-panel, .comparison-card) are applied per UI-SPEC section 5 /calibrate; replace the page header with the canonical SectionTitle (eyebrow/title/detail); keep loading/error/empty states via the canonical error-card/empty-state pattern; keep any token var() references intact (do not touch compliant ones).
    2. Run the gate script against the swept file until it exits 0; run frontend lint and build.
  </action>
  <verify>
    <automated>node scripts/check-banned-classes.mjs frontend/components/calibration/CalibrationWorkspace.tsx</automated>
    <automated>npm --prefix frontend run lint; if ($?) { npm --prefix frontend run build }</automated>
  </verify>
  <done>The gate script exists and works (flags violations, passes clean files, ignores comments); CalibrationWorkspace is fully token-clean with the canonical SectionTitle and calibration classes; lint and build pass.</done>
  <acceptance_criteria>
    - `node scripts/check-banned-classes.mjs frontend/components/calibration/CalibrationWorkspace.tsx` exits 0 with zero violations.
    - CalibrationWorkspace uses SectionTitle and the canonical calibration classes from UI-SPEC section 5; no per-page heading sizes or arbitrary fonts remain.
    - `npm --prefix frontend run lint` and `npm --prefix frontend run build` exit 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Token sweep of the remaining eight workspace components + typography/font/theme gates</name>
  <precondition>Task 1 green: gate script exists and CalibrationWorkspace is the proven token-clean pattern.</precondition>
  <reversibility rating="reversible">Each workspace is an independent local UI edit; reverting touches one file per workspace.</reversibility>
  <files>frontend/components/settings/SettingsWorkspace.tsx, frontend/components/contradictions/ContradictionWorkspace.tsx, frontend/components/functions/FunctionsWorkspace.tsx, frontend/components/developments/DevelopmentsWorkspace.tsx, frontend/components/missing-signals/MissingSignalsWorkspace.tsx, frontend/components/intelligence/AthenaWorkspace.tsx, frontend/components/observability/ActivityStreamWorkspace.tsx, frontend/components/signals/SignalList.tsx</files>
  <read_first>
    - frontend/components/settings/SettingsWorkspace.tsx (full file — credential/env sections; reference for the CONFIGURATION_ERROR credential display per UI-SPEC section 5 /settings)
    - frontend/components/contradictions/ContradictionWorkspace.tsx (filter buttons + badge tones per UI-SPEC section 5 /red-team)
    - frontend/components/functions/FunctionsWorkspace.tsx, frontend/components/developments/DevelopmentsWorkspace.tsx (timeline-track/timeline-node), frontend/components/missing-signals/MissingSignalsWorkspace.tsx (missingsignal-tint), frontend/components/intelligence/AthenaWorkspace.tsx (athena-card/athena-orbit/prompt-list/ask-row/answer-card/confidence), frontend/components/observability/ActivityStreamWorkspace.tsx (data-table-container), frontend/components/signals/SignalList.tsx (filter-bar/filter-active) — each per UI-SPEC section 5 rows
    - frontend/app/globals.css lines 31-88 (tokens) and frontend/components/metaradar.tsx lines 1089-1149 (canonical page pattern)
    - 08-UI-SPEC.md section 5 (Workspace Audit — Component by Component) and section 6 (Theme Persistence Requirements)
    - 08-RESEARCH.md lines 310-323 (verified violations per file) and "Pitfall 5" (lines 423-427)
  </read_first>
  <behavior>
    - Test 1: the gate script reports zero violations across all eight swept files.
    - Test 2: no swept file contains a hardcoded font-family declaration outside the canonical stack, and monospace is used only on identifier/technical-value spans.
    - Test 3: no swept file defines or reads per-page theme state (ThemeProvider remains the single source).
  </behavior>
  <action>
    Sweep each of the eight files using the Task 1 pattern (the gate script is the RED/GREEN oracle — run it per file as you go):

    1. SignalList.tsx: add the canonical SectionTitle (eyebrow "Ingested Signal Intelligence", title "Live Signals") per UI-SPEC section 5 /signals; migrate the filter bar to the canonical filter-bar button + filter-active classes; token-sweep the list rows to the .signal-row/.signal-copy/.signal-score pattern from metaradar.tsx SignalRow; keep the SignalCard provenance row from Plan 08-01 intact.
    2. SettingsWorkspace.tsx: token-sweep all surfaces/borders/text to the semantic tokens; add the credential status display per UI-SPEC section 5 /settings — render the configuration_error_message from the API (Plan 08-02 surfaced data) with the official location to obtain the key; never invent values; no hardcoded colors.
    3. ContradictionWorkspace.tsx: replace the inline danger/warning/medium badge styling with the canonical badge classes; migrate filter buttons to the filter-bar classes; confirm .redteam-tint/.contradiction-pair/.claim-box are applied.
    4. FunctionsWorkspace.tsx, DevelopmentsWorkspace.tsx, MissingSignalsWorkspace.tsx, AthenaWorkspace.tsx, ActivityStreamWorkspace.tsx: apply the canonical SectionTitle + .panel + .badge + .empty-state / .error-card patterns per UI-SPEC section 5 rows; replace all hardcoded colors with tokens; keep each workspace's canonical classes (.timeline-track/.timeline-node, .missingsignal-tint, .athena-card/.prompt-list/.answer-card, .data-table-container/.data-table) applied.
    5. Typography enforcement across all eight: no font-family declarations other than the canonical stack; no per-page heading sizes (headings via SectionTitle/.card-heading); monospace only on IDs, fingerprints, timestamps in technical values, and log values.
    6. Theme gates: confirm none of the swept files introduce theme state (no localStorage reads, no ThemeProvider duplication, no per-page light/dark toggles); confirm the only theme persistence remains the root provider.
    7. Run the gate script over all eight files until zero violations; run lint + build.
  </action>
  <verify>
    <automated>node scripts/check-banned-classes.mjs frontend/components/settings/SettingsWorkspace.tsx frontend/components/contradictions/ContradictionWorkspace.tsx frontend/components/functions/FunctionsWorkspace.tsx frontend/components/developments/DevelopmentsWorkspace.tsx frontend/components/missing-signals/MissingSignalsWorkspace.tsx frontend/components/intelligence/AthenaWorkspace.tsx frontend/components/observability/ActivityStreamWorkspace.tsx frontend/components/signals/SignalList.tsx</automated>
    <automated>npm --prefix frontend run lint; if ($?) { npm --prefix frontend run build }</automated>
  </verify>
  <done>All eight workspace components are token-clean with canonical headers, classes, typography, and font usage; Settings renders the CONFIGURATION_ERROR credential status; the gate script passes over all swept files; lint and build pass.</done>
  <acceptance_criteria>
    - `node scripts/check-banned-classes.mjs` over the eight files exits 0 with zero violations.
    - Grep gate: no hardcoded font-family declarations outside the canonical stack and no per-page theme state in the swept files (comment-filtered count of both patterns == 0).
    - SettingsWorkspace renders the credential status from the API configuration_error_message with an official-location link when a required key is missing.
    - `npm --prefix frontend run lint` and `npm --prefix frontend run build` exit 0.
    - SignalList has the canonical SectionTitle and filter-bar classes; no regressions in the Plan 08-01 provenance row on SignalCard.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full UI-SPEC section 10 manual verification matrix — both themes, all workspaces, drawers, telemetry, credentials</name>
  <what-built>The canonical design system sweep: nine token-clean workspace components (Calibration, Settings, Contradictions, Functions, Developments, Missing Signals, Intelligence, Observability, Signals list), the banned-class gate script, typography/font enforcement, and the credential status display — combined with the Plan 08-01 provenance UI and Plan 08-02 telemetry/credential surfaces.</what-built>
  <how-to-verify>
    1. Start the stack (docker compose up -d postgres redis; backend uvicorn; frontend next dev) and open http://localhost:3000 in dark mode. Walk UI-SPEC section 10 steps 1-15:
    - Step 1: all workspaces render with dark theme colors (no light artifacts).
    - Step 2: switch to light mode — all workspaces immediately use light colors; no dark artifacts anywhere.
    - Step 3: navigate 5 different workspaces — theme persists across all client-side navigations.
    - Step 4: refresh the browser — theme restored from storage; correct workspace loads.
    - Step 5: open a signal → EvidenceDrawer — SOURCE PROVENANCE populated with real data; drawer styled with the same tokens as Overview/Lifecycles.
    - Step 6: LIVE signal with PMID — Open Original Source opens the exact PubMed record in a new tab.
    - Step 7: LIVE signal with NCT ID — Open Original Source opens the exact ClinicalTrials.gov study.
    - Step 8: TEST FIXTURE signal — TEST FIXTURE badge (danger tone) + SOURCE URL UNAVAILABLE; never LIVE INTELLIGENCE.
    - Step 9: /confluence → open an alert — contributing evidence rows show provider, external ID, pub date, canonical URL or SOURCE URL UNAVAILABLE, excerpt; the copy matches the backend source-count rule; backward walk to the original source works.
    - Step 10: /sources — connector list shows telemetry (HTTP status, records fetched/accepted/rejected, last syncs, latency); if NEWSAPI_KEY is missing, the CONFIGURATION_ERROR: NEWSAPI_KEY missing badge (danger tone) is shown.
    - Step 11: /sources → trigger a sync — telemetry updates with real values.
    - Step 12: /signals → filter by severity — filter bar uses the canonical filter-active visual; results update.
    - Step 13: /red-team → filter by CRITICAL — contradiction items filter; badges use the canonical badge tones.
    - Step 14: direct URL to /settings — theme correct; settings renders cleanly and shows credential status for missing keys.
    - Step 15: repeat steps 1-14 in light mode — all pass (theme persists back and forth).
    2. Throughout: confirm no workspace shows hardcoded-color artifacts, no per-page font differences, and monospace appears only on IDs/technical values.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass in both themes, or describe which step(s) failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component CSS → tokens | Class hygiene crossing from hardcoded utilities into semantic tokens; a missed class breaks light mode |
| localStorage theme store → root provider | Theme value crosses storage into the provider; invalid values must degrade safely |
| swept components → API data | Settings/telemetry render API-provided credential/health data; must render state, never invent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12 | Tampering | all workspace components | medium | mitigate | CI-able gate script (scripts/check-banned-classes.mjs) scans every .tsx under frontend/components; lint + build gates run per wave |
| T-08-13 | Spoofing | ThemeProvider localStorage key | low | accept | Invalid stored theme values fall back to the default theme in the provider; cosmetic-only impact for an internal tool |
| T-08-14 | Spoofing (accessibility) | light-mode rendering | medium | mitigate | Sweep replaces every banned utility with dual-theme tokens; human verification matrix (Task 3) walks all 15 steps in BOTH themes |
| T-08-SC | Tampering | npm/pip installs | high | mitigate | Package legitimacy gate N/A — RESEARCH.md confirms no new packages; the gate script is zero-dependency node built-ins (fs/path) |
</threat_model>

<verification>
- `node scripts/check-banned-classes.mjs` over the nine swept files and, finally, over all of frontend/components exits 0 (comment-filtered count of banned utilities == 0).
- `npm --prefix frontend run lint` and `npm --prefix frontend run build` exit 0.
- Grep gates: no hardcoded font-family declarations outside the canonical stack; no per-page theme state; monospace limited to identifier/technical spans.
- Task 3 human checkpoint approves the full UI-SPEC section 10 matrix in both themes.
</verification>

<success_criteria>
- REQ-P8-08/10: all workspaces consume the Overview/Lifecycles tokens, SectionTitle hierarchy, and card/border/badge system.
- REQ-P8-09: single canonical font stack; monospace only on identifiers/technical values.
- REQ-P8-11/12: single theme store; persistence across navigation/refresh/drawer/direct URL; light is a real semantic theme.
- REQ-P8-13: EvidenceDrawer + Confluence inspector share the canonical drawer/modal treatment.
- REQ-P8-18/19: pytest/lint/build pass and the manual light/dark matrix is approved — completion is visual-behavior verified, not build-pass.
</success_criteria>

<output>
Create `.planning/phases/08-provenance-traceability-canonical-overview-lifecycle-design/08-03-SUMMARY.md` when done
</output>